/// <reference types="@figma/plugin-typings" />
import 'react';
// This file exists to ensure JSX intrinsic elements are recognized during build.
// The standard @types/react should already provide them; occasional TS resolution quirks in plugin bundling can surface.
